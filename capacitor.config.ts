import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sumsubionic.app',
  appName: 'sumsub-ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
